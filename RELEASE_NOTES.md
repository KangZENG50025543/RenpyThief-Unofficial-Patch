# v0.1.2 — 更新保护超时改为警告并继续

## 普通用户请下载这里

只需二选一，**不要两个都下载**：

| 文件 | 用途 |
|---|---|
| **`RenpyThiefPatch-v0.1.2-setup-x64.exe`** | 推荐；带安装向导、开始菜单、可选桌面快捷方式和卸载入口 |
| **`RenpyThiefPatch-v0.1.2-portable-x64.zip`** | 免安装；完整解压后运行 `RenpyThiefPatch.exe` |

`SHA256SUMS.txt` 用于校验下载文件。Release 中的 PyQt5、Qt、MinHook、Python 源码归档，以及 GitHub 自动显示的 **Source code (zip/tar.gz)**，均是供审计、开发和履行开源许可证义务使用的对应源码，**不是普通用户要安装的程序**。

本项目不包含 RenpyThief。当前只实测 **RenpyThief 6.7.8（x86 / Qt 5.15.2）**，仍需自行合法取得原版程序并按原版要求登录。本补丁不会绕过登录、注册、额度或付费授权。

## v0.1.2 更新内容

- 修复更新保护的启动策略：钩子已经就绪、RenpyThief 仍在运行，但 20 秒内没有观察到已知版本检查时，不再结束 RenpyThief。
- 上述情况现在会显示橙色“更新保护未确认”警告，并继续官方额度或自定义 API 启动流程；已注入的钩子仍会处理之后出现的已知版本接口。
- DLL 注入失败、钩子初始化失败、运行期 hook failed、等待 API 失败或目标进程提前退出，仍会报错并中止，不会被降级为警告。
- 官方额度与自定义 API 两条线路均支持该非致命警告，后续仍可正常进入“已就绪”。
- 加固启动器输出协议：成功 PID 与 warning 分离传递；输出异常时只对路径一致的精确 PID 执行清理，避免遗留未跟踪进程或误杀其他程序。
- 安装器与便携包沿用 v0.1.1 的开箱即用结构；依赖版本没有变化。

## 首次使用

1. 安装版完成向导后从桌面或开始菜单启动；便携版先完整解压，再运行 `RenpyThiefPatch.exe`。
2. 点击“浏览…”，选择你自己的 RenpyThief 6.7.8 x86 的 `RenpyThief.exe`。补丁不要安装或解压到原版目录。
3. 建议保持“拦截 RenpyThief 自动更新（推荐）”开启。
4. 选择“官方免费额度”，或选择“我的 API”并配置 Provider。使用自己的 API 时建议先点击“测试 API”。
5. 点击启动；如果原版要求登录，请正常完成登录。
6. **等待补丁明确显示“已就绪，可以拖入游戏”后，再把游戏拖入 RenpyThief。**

切换翻译线路前必须先关闭游戏和 RenpyThief，再从补丁重新启动；当前不支持热切换。

## API 与隐私提示

- DeepSeek、SiliconFlow 和 OpenAI-compatible 使用 API Key；有道、百度和 Microsoft 使用各自平台要求的凭据。
- “测试 API”只向所选平台发送固定文本 `こんにちは`，不会发送游戏内容。
- “我的 API”模式会把待翻译游戏文本发送给你选择的平台，并可能产生费用；请自行确认定价、配额和隐私政策。
- 只有在信任当前 Windows 账户和设备时，才选择将凭据保存到 Windows 凭据管理器。
- 不要在 Issue、截图或日志中公开 API Key、Cookie、账号信息或完整游戏文本。

## 重要说明与排错

- 安装器和程序尚未商业代码签名，Windows SmartScreen 或安全软件可能提示未知发布者。请只从本项目 GitHub Release 下载并用 `SHA256SUMS.txt` 校验；不要全局关闭安全软件。
- 一直未显示“已就绪”：关闭游戏和所有 RenpyThief/补丁实例，从补丁重新启动，确认原版窗口可用并完成登录。
- API 返回 `401/403`：检查凭据和权限；`404`：检查模型或 Base URL；`429`：检查余额、配额和速率限制；网络超时：检查网络、VPN/代理与平台地域可用性。
- 提示端口 `19899` 被占用：关闭旧的补丁或 Bridge 进程后重试。
- 仍无法解决时，点击 GUI 中“打开诊断目录”，仅提交经过人工检查和脱敏的相关日志片段。

详细说明见包内 `QUICK_START.txt`、`README.md` 和 `UPDATE_GUARD_CONTRACT.md`。

## 已知限制

- 仅实测 RenpyThief 6.7.8 x86，且仍依赖原版登录、引擎识别、游戏资源部署和注入流程。
- 不支持运行中切换线路，不自动检测官方额度耗尽，也不会自动回退到其他 Provider。
- 更新保护只匹配已知版本检查，不能保证未来任意版本自动兼容。
- 若出现“更新保护未确认”，表示确认窗口内没有看到已知版本接口，并不等于保护已成功，也不等于更新请求已经放行；未来版本仍可能不兼容。
- 翻译质量、速率、价格和地域限制主要由所选模型或平台决定。

## 许可证与对应源码

项目源码采用 `GPL-3.0-only`，第三方组件适用各自许可证。`v0.1.2` 标签对应本次安装版和便携版的补丁源码；Release 同时提供 PyQt5 5.15.11、Qt 5.15.2、MinHook 1.3.4 和 CPython 3.12.7 的锁定源码归档及 `SOURCE_ARCHIVES.SHA256`。这些源码附件面向开发者和审计者，普通用户无需下载。
